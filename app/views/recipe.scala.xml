@(rec: Recipe)

<recipe id="@rec.getId()">
	<name>@rec.getName</name>
	<createdAt>@rec.getDateCreation</createdAt>
	<ingredients>
	@for(i  <- rec.getIngredients){
		@ingredient(i)
	}
	</ingredients>
	<tags>
	@for(t  <- rec.getTags){
		@tag(t)
	}
	</tags>
</recipe>