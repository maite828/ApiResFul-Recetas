@(recipes: List[Recipe])
<!-- lista -->
<recipes>
	@for(item <- recipes){
		<recipe id="@item.getId()">
			<name>@item.getName</name>
			<createdAt>@item.getDateCreation</createdAt>	
		</recipe>
	}
</recipes>	




<!-- detalle -->
<recipes>
	@for(r <- recipes){
		@recipe(r)
	}
</recipes>