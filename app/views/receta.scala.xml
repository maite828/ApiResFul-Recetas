@(receta: Receta)

<receta id="@receta.getId()">
	<name>@receta.getName</name>
	<createdAt>@receta.getDateCreation</createdAt>
	<recIngrediente>
	@for(i <- receta.getRecIngredientes){
	@i  
	}
	</recIngrediente>
</receta>