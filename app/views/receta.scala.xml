@(rec: Receta)

<receta id="@rec.getId()">
	<name>@rec.getName</name>
	<createdAt>@rec.getDateCreation</createdAt>
	<ingredientes>
	@for(item  <- rec.getIngredientes){
		@ingrediente(item)
	}
	</ingredientes>
</receta>